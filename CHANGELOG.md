# Changelog

## 0.1.1 - 2026-04-13

- Removed hardcoded private/internal network defaults from server code and service files.
- Replaced private tailnet host examples in README with placeholders.
- Switched documented startup commands from `enable --now` to manual `start` to avoid auto-start by default.
- Added `.gitignore` entries for Python bytecode and runtime database artifacts.
